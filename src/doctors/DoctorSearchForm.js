import React, { Component } from 'react'
import { Form } from "semantic-ui-react"

export default class componentName extends Component {

    state = {
        api_key = process.env.BETTER_DOCTORS_API_KEY
    }

    render() {
        return (
        <div>
          WEEEEEEEEEE!!!!!!!!  
        </div>
        )
    }
}
